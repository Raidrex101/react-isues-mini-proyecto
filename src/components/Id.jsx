import React from 'react'

function Id({id}) {
  return (
    <div style={{borderRadius: '10px', backgroundColor: 'grey', padding: '10px'}}>
        <span>{id}</span>
    </div>
  )
}

export default Id
